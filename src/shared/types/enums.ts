export enum USER_ROLE {
    ADMIN = 'ADMIN',
    GUEST = 'GUEST',
    USER = 'USER',
}

export enum USER_STATUS {
    ACTIVE = 'ACTIVE',
    BLOCKED = 'BLOCKED',
    FROZEN = 'FROZEN',
    INACTIVE = 'INACTIVE',
    DELETED = 'DELETED',
}
